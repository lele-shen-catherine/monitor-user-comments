---
name: monitor-user-comments
description: 规范化并分析来源明确的抖音帖子与用户评论样本，识别用户关心点和可复刻玩梗，每次生成可离线打开的可视化页面和人工复核表。用于领域、话题、达人或指定帖子的内容监测；不用于小红书或绕过平台限制采集数据。
metadata:
  version: "2.6.0"
  release-date: "2026-09-13"
---

# 用户评论监测

## 目标

把“合规数据 → 规范化 → 用户关心点 → 可追溯报告”做成稳定流程。情绪是辅助字段，不把负面比例直接当成业务结论。

## 依赖

供普通使用者使用的一键入口：

- macOS：双击 `启动.command`
- Windows：双击 `启动.bat`

一键入口通过 uv 自动配置 Python 3.11，使用仓库内的 SnowNLP 离线 wheel，并从官方仓库下载固定版本的 MediaCrawler 及其 Node.js 运行时。电脑需已安装 Chrome 或 Edge，采集器会自动调用本机浏览器并复用登录状态。首次下载前必须由使用者接受 MediaCrawler 的非商业学习许可证。详细来源和授权见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

手动安装分析依赖：

```bash
python3 -m pip install -r scripts/requirements.txt
```

需要 Python 3.11+。分析脚本只依赖标准库和 SnowNLP，不依赖本项目其他代码。

## 先确定监测对象

- `domain`：领域；关键词应覆盖主要子问题。
- `topic`：具体话题；可配置同义词和排除词。
- `creator`：区分“达人发布内容下的评论”和“全平台讨论达人”。前者必须用达人 ID 或帖子链接，不能只用昵称。
- `post`：一个或多个明确帖子链接。

详细字段和边界见 [references/target-schema.md](references/target-schema.md)。

## 数据来源（三选一）

1. 用户提供的授权导出文件：MediaCrawler 风格 JSONL，目录结构为 `dy/jsonl/` 或 `douyin/jsonl/`，至少包含 `*contents_*.jsonl` 和 `*comments_*.jsonl`。
2. 工作区已有的合规导出：使用前检查批次目录、关键词和覆盖类型，禁止把不同批次混在一起。
3. 官方接口 / 授权供应商 / 后台导出：先确认字段与覆盖范围，再按同一 JSONL 结构整理。

不得绕过登录、验证码、访问控制、风控、robots.txt 或平台条款；不得把公开搜索样本称为全平台完整 Top 100；不得在报告或日志中泄露 Cookie、Token、手机号等敏感信息。

## 执行步骤

### 1. 确认关键词与平台

- 关键词不设固定名单。可以从用户对话中提取，也可以由用户在本地前端搜索框输入。
- 只监测抖音；默认时间：执行日向前 180 天与 7 天。
- 抖音搜索应使用“最高点赞”排序，且只把结果当候选池，最终必须在本地按实际点赞/互动排序。

### 2. 从本地前端启动采集

已有 MediaCrawler 且使用者有权采集相应公开数据时，可以启动本地控制页面：

```bash
python scripts/run_ui.py \
  --collector-root /path/to/MediaCrawler \
  --workspace runs \
  --results reports
```

打开 `http://127.0.0.1:8765`，在搜索框输入任意关键词并开始监测。默认先执行快速监测：最多读取 20 条高赞帖子、每帖最多 50 条一级评论；完成后自动生成结果页面。结果页提供“深度监测”，由用户主动选择后才扩展到最多 100 条帖子、每帖 100 条评论。

首次运行可能需要使用者在浏览器中完成抖音登录或验证。采集器通过自己的本机持久浏览器目录保存登录状态，后续任务优先复用；状态失效时才重新登录。Skill 不读取、展示、上传或打包该目录中的 Cookie 和 Token。

采集器必须作为独立、已安装且获准使用的依赖提供。不得绕过登录、验证码、访问控制或风控。安装器仅从 MediaCrawler 官方仓库获取固定版本并保留原版权和许可证，不把第三方源码冒充为本项目代码。MediaCrawler 仅供非商业学习研究；商业使用前必须获得版权所有者书面许可或改用官方接口、企业后台导出或合规供应商。若没有采集器，则继续使用授权导出数据。

### 3. 规范化、分析与复核

```bash
python scripts/analyze_export.py \
  --export-root /path/to/data-root \
  --keywords "七夕,京东外卖" \
  --output reports/weekly-dy.html \
  --markdown-output reports/weekly-dy.md \
  --review-output reports/weekly-dy-review.csv
```

参数：

- `--platform`：固定为 `dy`，保留参数仅为兼容旧命令
- `--keywords`：英文逗号分隔
- `--six-month-days`、`--seven-day-days`：窗口天数，默认 180 / 7
- `--post-limit`、`--comment-limit`：默认 100
- `--comment-candidate-limit`：默认 500
- `--target-type`：`domain` / `topic` / `creator` / `post`
- `--creator-id`、`--creator-scope`、`--post-url`：限定达人或帖子
- `--synonyms`、`--exclude-keywords`：扩展和排除检索语义
- `--like-weight`、`--comment-weight`：默认 1.0 / 2.0
- `--timezone`：默认 Asia/Shanghai

### 4. 生成页面

每次执行必须生成一个自包含 HTML 页面，默认包含“抖音近6个月”和“抖音近7天”两个可切换视图。页面顶部保留关键词搜索框：通过本地控制页面打开时可直接创建下一次监测；直接双击静态 HTML 时只显示结果并提示通过启动器搜索。Markdown 仅作为可选追溯附件。

报告包含：

- 数据概览与情绪占比（正面/中性/负面/混合）
- 高频主题 Top 10（已去停用词与表情词）
- 可复刻玩梗 Top 10：从高赞帖子及其高赞评论中识别，展示表达模板、复刻公式、玩法、帖子/评论来源、互动热度和脱敏证据
- 按本地生活板块聚类的核心观点与原生评论证据
- 高赞帖子 Top 10 与爆款原因（标“推测”）
- 业务建议表：板块 → 证据 → 根因假设 → 建议动作 → 负责团队 → 优先级 → 验证指标
- 跨时间窗趋势和优先行动清单
- 口径与来源附录

## 业务建议聚类

负面评论按以下板块聚类，见 `references/local-life-clusters.md`：

外卖履约时效、餐品与质量、价格与优惠、客服与售后、即时零售缺货与库存、家政服务质量、咖啡与茶饮体验、履约物流与包裹。

## 质量门槛

- 帖子数按平台+帖子 ID 去重；评论数按平台+评论 ID 去重，仅保留一级评论。
- 情绪占比分母为该板块全部有效评论；不足 30 条时显著标注“小样本，仅供定性参考”。
- 关心点至少由 3 条一致评论支持；仅 1–2 条时标“弱信号”，不得泛化。
- 正式报告发布前，复核每个平台/时间窗最多 30 条随机稳定样本；不足 30 条时全检，并在 CSV 中填写人工标签。
- 高频主题不足 10 项按实输出，不补造。
- 玩梗必须能够抽象成可替换、可模仿的表达公式；普通反应语、高频复读、纯表情、@互动和求图不作为梗。候选帖子只按点赞量降序选取，候选评论只从这些帖子下按点赞量降序选取。帖子互动基数为“点赞 + 评论 × 2”，评论互动基数为点赞，最终热度分为“互动基数 + 内容提及数 × 5”，并保留来源与代表内容供复核。
- 缺少来源链接、发布时间或互动量时，不进行依赖该字段的排名或趋势结论。
- 报告区分事实、分析判断与推测，并披露数据覆盖边界。
